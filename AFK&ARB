-- [[ 🔴 AFK&ARB V5 - VIP EDITION 🔴 ]] --

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

-- [[ 1. نظام الحماية (Whitelist) ]] --
local WhitelistedPlayers = {
    ["azozh1500111"] = true,
    ["yassin_3mk033"] = true
}

if not WhitelistedPlayers[LocalPlayer.Name] then
    LocalPlayer:Kick("⛔ عذراً! هذا السكربت خاص ومصرح فقط لـ azozh1500111 و yassin_3mk033")
    return
end

-- [[ 2. صائد الريموتات الذكي ]] --
local DetectedRemotes = {}
local function ScanForRemotes()
    table.clear(DetectedRemotes)
    local scanAreas = { game:GetService("ReplicatedStorage"), LocalPlayer.Backpack, LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() }
    for _, area in ipairs(scanAreas) do
        for _, obj in pairs(area:GetDescendants()) do
            if obj:IsA("RemoteEvent") then
                local isDefaultChat = false
                pcall(function()
                    if obj:IsDescendantOf(game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")) then
                        isDefaultChat = true
                    end
                end)
                if not isDefaultChat then
                    table.insert(DetectedRemotes, obj)
                end
            end
        end
    end
end

-- [[ 3. إعدادات الواجهة (GUI) ]] --
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AFK_ARB_VIP"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

-- زر الفتح
local OpenBtn = Instance.new("TextButton")
OpenBtn.Size = UDim2.new(0, 60, 0, 60)
OpenBtn.Position = UDim2.new(0, 20, 0, 20)
OpenBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
OpenBtn.Text = "AFK"
OpenBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
OpenBtn.Font = Enum.Font.GothamBold
OpenBtn.TextSize = 20
OpenBtn.Parent = ScreenGui
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", OpenBtn).Color = Color3.fromRGB(255, 0, 0)
Instance.new("UIStroke", OpenBtn).Thickness = 2.5

-- الإطار الرئيسي
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 700, 0, 480)
MainFrame.Position = UDim2.new(0.5, -350, 0.5, -240)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 15)
Instance.new("UIStroke", MainFrame).Color = Color3.fromRGB(200, 0, 0)
Instance.new("UIStroke", MainFrame).Thickness = 3

OpenBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

-- [[ 4. الحركة الجمالية (الهيدر والصور) ]] --
local HeaderFrame = Instance.new("Frame")
HeaderFrame.Size = UDim2.new(1, 0, 0, 100)
HeaderFrame.BackgroundTransparency = 1
HeaderFrame.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = "AFK&ARB"
Title.TextColor3 = Color3.fromRGB(255, 0, 0)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 45
Title.Parent = HeaderFrame

local LeftAvatar = Instance.new("ImageLabel")
LeftAvatar.Size = UDim2.new(0, 90, 0, 90)
LeftAvatar.Position = UDim2.new(0, 15, 0, 5)
LeftAvatar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
LeftAvatar.Parent = HeaderFrame
Instance.new("UICorner", LeftAvatar).CornerRadius = UDim.new(1, 0)

local RightAvatar = Instance.new("ImageLabel")
RightAvatar.Size = UDim2.new(0, 90, 0, 90)
RightAvatar.Position = UDim2.new(1, -105, 0, 5)
RightAvatar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
RightAvatar.Parent = HeaderFrame
Instance.new("UICorner", RightAvatar).CornerRadius = UDim.new(1, 0)

-- جلب صور اللاعبين مباشرة من روبلوكس
task.spawn(function()
    pcall(function()
        local id1 = Players:GetUserIdFromNameAsync("azozh1500111")
        LeftAvatar.Image = Players:GetUserThumbnailAsync(id1, Enum.ThumbnailType.AvatarBust, Enum.ThumbnailSize.Size420x420)
    end)
    pcall(function()
        local id2 = Players:GetUserIdFromNameAsync("yassin_3mk033")
        RightAvatar.Image = Players:GetUserThumbnailAsync(id2, Enum.ThumbnailType.AvatarBust, Enum.ThumbnailSize.Size420x420)
    end)
end)

local Divider = Instance.new("Frame")
Divider.Size = UDim2.new(1, 0, 0, 3)
Divider.Position = UDim2.new(0, 0, 0, 105)
Divider.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Divider.BorderSizePixel = 0
Divider.Parent = MainFrame

-- [[ 5. المتغيرات والأوامر ]] --
local selectedPlayer = LocalPlayer.Name
local selectedMode = nil -- يحفظ الاختيار (مشرف أو هيد ادمن)
local isSending = false

local Commands = {
    Mod = "@cmdbar [TARGET] @logs [TARGET] @re [TARGET] @kill [TARGET] @nv [TARGET] @ice [TARGET] @jc [TARGET] @paint [TARGET] @worm [TARGET] @char [TARGET] c7v @tp [TARGET]",
    AdminMystery = "?EXPLODE [TARGET] ?WARP [TARGET] ?JAIL [TARGET] ?ICE [TARGET] ?LOOPWARF [TARGET] ?LOGS [TARGET] ?CLOGS [TARGET] ?CHATLOGS [TARGET] ?NV [TARGET] ?CMDBAR [TARGET] ?CHAR [TARGET] MIR' ?ALERT [TARGET] ?AURA [TARGET] ?EXPLODE"
}

-- [[ 6. اختيار اللاعبين (اليمين) ]] --
local PlayerFrame = Instance.new("Frame")
PlayerFrame.Size = UDim2.new(0, 300, 0, 240)
PlayerFrame.Position = UDim2.new(1, -320, 0, 120)
PlayerFrame.BackgroundTransparency = 1
PlayerFrame.Parent = MainFrame

local TargetLabel = Instance.new("TextLabel")
TargetLabel.Size = UDim2.new(1, 0, 0, 30)
TargetLabel.BackgroundTransparency = 1
TargetLabel.Text = "اللاعب المحدد: " .. selectedPlayer
TargetLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TargetLabel.Font = Enum.Font.GothamBold
TargetLabel.TextSize = 16
TargetLabel.Parent = PlayerFrame

local PlayerScroll = Instance.new("ScrollingFrame")
PlayerScroll.Size = UDim2.new(1, 0, 1, -40)
PlayerScroll.Position = UDim2.new(0, 0, 0, 40)
PlayerScroll.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
PlayerScroll.ScrollBarThickness = 8
PlayerScroll.Parent = PlayerFrame
Instance.new("UICorner", PlayerScroll).CornerRadius = UDim.new(0, 8)
local PListLayout = Instance.new("UIListLayout", PlayerScroll)
PListLayout.Padding = UDim.new(0, 5)

local function RefreshPlayers()
    for _, v in pairs(PlayerScroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    for _, p in pairs(Players:GetPlayers()) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, -12, 0, 40)
        btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        btn.Text = "🎯 " .. p.Name
        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 15
        btn.Parent = PlayerScroll
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        
        btn.MouseButton1Click:Connect(function()
            selectedPlayer = p.Name
            TargetLabel.Text = "اللاعب المحدد: " .. p.Name
        end)
    end
end
RefreshPlayers()
Players.PlayerAdded:Connect(RefreshPlayers)
Players.PlayerRemoving:Connect(RefreshPlayers)

-- [[ 7. اختيار نوع النسخ (اليسار) ]] --
local ModesFrame = Instance.new("Frame")
ModesFrame.Size = UDim2.new(0, 320, 0, 200)
ModesFrame.Position = UDim2.new(0, 20, 0, 140)
ModesFrame.BackgroundTransparency = 1
ModesFrame.Parent = MainFrame

local ModeTitle = Instance.new("TextLabel")
ModeTitle.Size = UDim2.new(1, 0, 0, 30)
ModeTitle.BackgroundTransparency = 1
ModeTitle.Text = "اختر نوع النسخ:"
ModeTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ModeTitle.Font = Enum.Font.GothamBold
ModeTitle.TextSize = 18
ModeTitle.Parent = ModesFrame

local ModBtn = Instance.new("TextButton")
ModBtn.Size = UDim2.new(1, 0, 0, 50)
ModBtn.Position = UDim2.new(0, 0, 0, 50)
ModBtn.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
ModBtn.Text = "نسخ مشرف 👑"
ModBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
ModBtn.Font = Enum.Font.GothamBold
ModBtn.TextSize = 18
ModBtn.Parent = ModesFrame
Instance.new("UICorner", ModBtn).CornerRadius = UDim.new(0, 8)
local ModStroke = Instance.new("UIStroke", ModBtn)
ModStroke.Color = Color3.fromRGB(255, 0, 0)

local AdminBtn = Instance.new("TextButton")
AdminBtn.Size = UDim2.new(1, 0, 0, 50)
AdminBtn.Position = UDim2.new(0, 0, 0, 115)
AdminBtn.BackgroundColor3 = Color3.fromRGB(40, 15, 15)
AdminBtn.Text = "نسخ هيد ادمن و غامض 💀👁️"
AdminBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
AdminBtn.Font = Enum.Font.GothamBold
AdminBtn.TextSize = 18
AdminBtn.Parent = ModesFrame
Instance.new("UICorner", AdminBtn).CornerRadius = UDim.new(0, 8)
local AdminStroke = Instance.new("UIStroke", AdminBtn)
AdminStroke.Color = Color3.fromRGB(255, 0, 0)

-- دالة التحديد وتغيير اللون
local function SelectMode(mode)
    selectedMode = mode
    ModBtn.BackgroundColor3 = (mode == "Mod") and Color3.fromRGB(200, 0, 0) or Color3.fromRGB(40, 15, 15)
    ModBtn.TextColor3 = (mode == "Mod") and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 100, 100)
    
    AdminBtn.BackgroundColor3 = (mode == "AdminMystery") and Color3.fromRGB(200, 0, 0) or Color3.fromRGB(40, 15, 15)
    AdminBtn.TextColor3 = (mode == "AdminMystery") and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 100, 100)
end

ModBtn.MouseButton1Click:Connect(function() SelectMode("Mod") end)
AdminBtn.MouseButton1Click:Connect(function() SelectMode("AdminMystery") end)

-- [[ 8. أزرار التشغيل والإيقاف (تحت) ]] --
local StartBtn = Instance.new("TextButton")
StartBtn.Size = UDim2.new(0, 300, 0, 55)
StartBtn.Position = UDim2.new(0, 20, 0, 400)
StartBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0) -- أخضر
StartBtn.Text = "بدء النسخ ▶️"
StartBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
StartBtn.Font = Enum.Font.GothamBlack
StartBtn.TextSize = 22
StartBtn.Parent = MainFrame
Instance.new("UICorner", StartBtn).CornerRadius = UDim.new(0, 10)

local StopBtn = Instance.new("TextButton")
StopBtn.Size = UDim2.new(0, 300, 0, 55)
StopBtn.Position = UDim2.new(1, -320, 0, 400)
StopBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0) -- أحمر
StopBtn.Text = "توقيف النسخ 🛑"
StopBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
StopBtn.Font = Enum.Font.GothamBlack
StopBtn.TextSize = 22
StopBtn.Parent = MainFrame
Instance.new("UICorner", StopBtn).CornerRadius = UDim.new(0, 10)

-- [[ 9. نظام الإرسال المستمر ]] --
StartBtn.MouseButton1Click:Connect(function()
    if not selectedMode then
        ModeTitle.Text = "⚠️ الرجاء تحديد نوع النسخ أولاً!"
        task.wait(2)
        ModeTitle.Text = "اختر نوع النسخ:"
        return
    end
    
    isSending = false
    task.wait(0.1)
    isSending = true
    
    -- تجهيز الأمر وتبديل [TARGET] باسم اللاعب المختار
    local finalCmd = string.gsub(Commands[selectedMode], "%[TARGET%]", selectedPlayer)
    
    ScanForRemotes()
    
    task.spawn(function()
        while isSending do
            for _, remote in ipairs(DetectedRemotes) do
                pcall(function()
                    remote:FireServer(finalCmd)
                end)
            end
            task.wait(1) -- سرعة الإرسال (كل ثانية)
        end
    end)
end)

StopBtn.MouseButton1Click:Connect(function()
    isSending = false
end)
