local Players = game:GetService("Players")
local TextChatService = game:GetService("TextChatService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- إنشاء الشاشة الرئيسية
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AbodScriptUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- النافذة الرئيسية
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 620, 0, 440)
MainFrame.Position = UDim2.new(0.5, -310, 0.5, -220)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 22)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- الشريط العلوي (العنوان)
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -20, 1, 0)
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.Text = "تم تشغيل سكربت عبود"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 18
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.BackgroundTransparency = 1
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

-- القائمة الجانبية (الأزرار على اليسار)
local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 160, 1, -40)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainFrame

local sidebarLayout = Instance.new("UIListLayout")
sidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder
sidebarLayout.Padding = UDim.new(0, 4)
sidebarLayout.Parent = Sidebar

-- منطقة المحتوى الرئيسي
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -160, 1, -40)
ContentFrame.Position = UDim2.new(0, 160, 0, 40)
ContentFrame.BackgroundColor3 = Color3.fromRGB(28, 28, 32)
ContentFrame.BorderSizePixel = 0
ContentFrame.Parent = MainFrame

-- دالة إرسال الأوامر عبر الشات
local function sendChat(msg)
    if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
        local generalChannel = TextChatService.TextChannels:FindFirstChild("RBXGeneral")
        if generalChannel then
            generalChannel:SendAsync(msg)
        end
    else
        local chatEvent = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
        if chatEvent and chatEvent:FindFirstChild("SayMessageRequest") then
            chatEvent.SayMessageRequest:FireServer(msg, "All")
        end
    end
end

-- إدارة الصفحات
local tabs = {}
local tabButtons = {}

local function createTabFrame(name)
    local tabFrame = Instance.new("ScrollingFrame")
    tabFrame.Size = UDim2.new(1, -20, 1, -20)
    tabFrame.Position = UDim2.new(0, 10, 0, 10)
    tabFrame.BackgroundTransparency = 1
    tabFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    tabFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
    tabFrame.ScrollBarThickness = 5
    tabFrame.Visible = false
    tabFrame.Parent = ContentFrame
    
    local listLayout = Instance.new("UIListLayout")
    listLayout.SortOrder = Enum.SortOrder.LayoutOrder
    listLayout.Padding = UDim.new(0, 8)
    listLayout.Parent = tabFrame
    
    tabs[name] = tabFrame
    return tabFrame
end

local tabList = {
    "تخريب على الكل",
    "النسخ",
    "الحماية",
    "Cmdbar 2",
    "سكربتات إضافية",
    "الريموتات"
}

for idx, name in ipairs(tabList) do
    local frame = createTabFrame(name)
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 38)
    btn.Position = UDim2.new(0, 5, 0, 0)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 15
    btn.BorderSizePixel = 0
    btn.Parent = Sidebar
    
    btn.MouseButton1Click:Connect(function()
        for _, t in pairs(tabs) do t.Visible = false end
        for _, b in pairs(tabButtons) do 
            b.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
            b.TextColor3 = Color3.fromRGB(200, 200, 200)
        end
        frame.Visible = true
        btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end)
    
    table.insert(tabButtons, btn)
end

-- تفعيل القائمة الأولى افتراضياً
tabs["تخريب على الكل"].Visible = true
tabButtons[1].BackgroundColor3 = Color3.fromRGB(45, 45, 55)
tabButtons[1].TextColor3 = Color3.fromRGB(255, 255, 255)

-- دالة إنتاج الأزرار الملونة
local function createActionButton(parent, text, bgColor, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 42)
    btn.BackgroundColor3 = bgColor
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 15
    btn.BorderSizePixel = 0
    btn.Parent = parent
    
    btn.MouseButton1Click:Connect(callback)
    return btn
end

---------------------------------------------------------
-- 1. قائمة تخريب على الكل
---------------------------------------------------------
local tab1 = tabs["تخريب على الكل"]

-- تشغيل سبام السيرفر التدريجي الجبار (بنفسجي) 🟣
createActionButton(tab1, "🟣 تشغيل سبام السيرفر التدريجي الجبار", Color3.fromRGB(128, 0, 128), function()
    task.spawn(function()
        for _, p in ipairs(Players:GetPlayers()) do
            sendChat(";kill " .. p.Name)
            task.wait(0.15)
            sendChat(";jail " .. p.Name)
            task.wait(0.15)
        end
    end)
end)

-- إبادة السيرفر (أحمر) 🔴
createActionButton(tab1, "🔴 إبادة السيرفر", Color3.fromRGB(200, 35, 35), function()
    for _, p in ipairs(Players:GetPlayers()) do
        sendChat(";kill " .. p.Name)
        sendChat(";void " .. p.Name)
    end
end)

-- تحويل الكل لديدان (أخضر) 🟢
createActionButton(tab1, "🟢 تحويل الكل لديدان", Color3.fromRGB(35, 170, 50), function()
    for _, p in ipairs(Players:GetPlayers()) do
        sendChat(";worm " .. p.Name)
        sendChat(";wormify " .. p.Name)
    end
end)

-- تجميد الكل (أزرق) 🔵
createActionButton(tab1, "🔵 تجميد الكل", Color3.fromRGB(30, 130, 220), function()
    for _, p in ipairs(Players:GetPlayers()) do
        sendChat(";freeze " .. p.Name)
    end
end)

-- سجن الكل (رصاصي) 🩶
createActionButton(tab1, "🩶 سجن الكل", Color3.fromRGB(110, 110, 110), function()
    for _, p in ipairs(Players:GetPlayers()) do
        sendChat(";jail " .. p.Name)
    end
end)

-- إعطاء تايتل مخصص للكل (أزرق غامق وتحته التايتل) 🟦
local titleGroup = Instance.new("Frame")
titleGroup.Size = UDim2.new(1, 0, 0, 85)
titleGroup.BackgroundTransparency = 1
titleGroup.Parent = tab1

local titleBtn = createActionButton(titleGroup, "🟦 إعطاء تايتل مخصص للكل", Color3.fromRGB(10, 30, 100), function() end)
titleBtn.Size = UDim2.new(1, 0, 0, 40)

local titleBox = Instance.new("TextBox")
titleBox.Size = UDim2.new(1, 0, 0, 38)
titleBox.Position = UDim2.new(0, 0, 0, 45)
titleBox.PlaceholderText = "اكتب التايتل هنا..."
titleBox.Text = ""
titleBox.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
titleBox.TextColor3 = Color3.fromRGB(255, 255, 255)
titleBox.Font = Enum.Font.SourceSans
titleBox.TextSize = 14
titleBox.BorderSizePixel = 0
titleBox.Parent = titleGroup

titleBtn.MouseButton1Click:Connect(function()
    local text = titleBox.Text
    if text ~= "" then
        for _, p in ipairs(Players:GetPlayers()) do
            sendChat(";title " .. p.Name .. " " .. text)
        end
    end
end)

-- قذف وتطيير الكل تدريجيا (وردي) 🩷
createActionButton(tab1, "🩷 قذف وتطيير الكل تدريجياً", Color3.fromRGB(235, 90, 170), function()
    task.spawn(function()
        for _, p in ipairs(Players:GetPlayers()) do
            sendChat(";fling " .. p.Name)
            task.wait(0.2)
        end
    end)
end)

---------------------------------------------------------
-- 2. قائمة النسخ
---------------------------------------------------------
local tab2 = tabs["النسخ"]
local selectedPlayerName = ""

local listContainer = Instance.new("ScrollingFrame")
listContainer.Size = UDim2.new(1, 0, 0, 160)
listContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
listContainer.BorderSizePixel = 0
listContainer.ScrollBarThickness = 4
listContainer.Parent = tab2

local playerListLayout = Instance.new("UIListLayout")
playerListLayout.SortOrder = Enum.SortOrder.LayoutOrder
playerListLayout.Padding = UDim.new(0, 3)
playerListLayout.Parent = listContainer

local function updatePlayerList()
    for _, child in ipairs(listContainer:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    
    for _, p in ipairs(Players:GetPlayers()) do
        local pBtn = Instance.new("TextButton")
        pBtn.Size = UDim2.new(1, 0, 0, 28)
        pBtn.BackgroundColor3 = (selectedPlayerName == p.Name) and Color3.fromRGB(0, 120, 215) or Color3.fromRGB(35, 35, 40)
        pBtn.Text = " 👤 " .. p.Name .. " (" .. p.DisplayName .. ")"
        pBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        pBtn.Font = Enum.Font.SourceSans
        pBtn.TextSize = 14
        pBtn.TextXAlignment = Enum.TextXAlignment.Left
        pBtn.BorderSizePixel = 0
        pBtn.Parent = listContainer
        
        pBtn.MouseButton1Click:Connect(function()
            selectedPlayerName = p.Name
            updatePlayerList()
        end)
    end
end

updatePlayerList()
Players.PlayerAdded:Connect(updatePlayerList)
Players.PlayerRemoving:Connect(updatePlayerList)

local copyCmds = {
    ";jail", ";jail2", ";freeze", ";kill", ";loopkill", ";respawn", ";refresh", 
    ";void", ";fire", ";bring", ";to", ";teleport", ";handTo", ";box", ";dog", 
    ";worm", ";wormify", ";chibify", ";plushify", ";freaky", ";frogly", ";spongify", 
    ";bigify", ";creepify", ";dinofy", ";fatify", ";ghost", ";squash", ";fat", 
    ";thin", ";giantDwarf", ";dwarf", ";headSize", ";bodyTypeScale", ";depth", 
    ";potatoHead", ";char", ";cmdbar", ";logs", ";chatLogs"
}

createActionButton(tab2, "⚡ ابدأ النسخ", Color3.fromRGB(0, 150, 75), function()
    if selectedPlayerName ~= "" then
        task.spawn(function()
            for _, cmd in ipairs(copyCmds) do
                sendChat(cmd .. " " .. selectedPlayerName)
                task.wait(0.12)
            end
        end)
    end
end)

---------------------------------------------------------
-- 3. قائمة الحماية (مثالية)
---------------------------------------------------------
local tab3 = tabs["الحماية"]
local protInfo = Instance.new("TextLabel")
protInfo.Size = UDim2.new(1, 0, 0, 40)
protInfo.Text = "🛡️ نظام الحماية الكلي نشط وبأعلى كفاءة"
protInfo.TextColor3 = Color3.fromRGB(0, 230, 120)
protInfo.BackgroundTransparency = 1
protInfo.Font = Enum.Font.SourceSansBold
protInfo.TextSize = 16
protInfo.Parent = tab3

---------------------------------------------------------
-- 4. قائمة Cmdbar 2
---------------------------------------------------------
local tab4 = tabs["Cmdbar 2"]
local cmdInput = Instance.new("TextBox")
cmdInput.Size = UDim2.new(1, 0, 0, 42)
cmdInput.PlaceholderText = "اكتب الأمر هنا واضغط Enter..."
cmdInput.Text = ""
cmdInput.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
cmdInput.TextColor3 = Color3.fromRGB(255, 255, 255)
cmdInput.Font = Enum.Font.SourceSans
cmdInput.TextSize = 14
cmdInput.BorderSizePixel = 0
cmdInput.Parent = tab4

cmdInput.FocusLost:Connect(function(enterPressed)
    if enterPressed and cmdInput.Text ~= "" then
        sendChat(cmdInput.Text)
        cmdInput.Text = ""
    end
end)

---------------------------------------------------------
-- 5. قائمة سكربتات إضافية
---------------------------------------------------------
local tab5 = tabs["سكربتات إضافية"]
local extraLabel = Instance.new("TextLabel")
extraLabel.Size = UDim2.new(1, 0, 0, 30)
extraLabel.Text = "📜 قسم السكربتات الإضافية"
extraLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
extraLabel.BackgroundTransparency = 1
extraLabel.Font = Enum.Font.SourceSans
extraLabel.TextSize = 14
extraLabel.Parent = tab5

---------------------------------------------------------
-- 6. قائمة الريموتات
---------------------------------------------------------
local tab6 = tabs["الريموتات"]
local remoteLabel = Instance.new("TextLabel")
remoteLabel.Size = UDim2.new(1, 0, 0, 30)
remoteLabel.Text = "📡 قسم مراقبة وإرسال الريموتات (RemoteEvents)"
remoteLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
remoteLabel.BackgroundTransparency = 1
remoteLabel.Font = Enum.Font.SourceSans
remoteLabel.TextSize = 14
remoteLabel.Parent = tab6
