-- // ================================================================= //
-- //          👑 ABOOD ULTIMATE HUB V30 - SPECIAL EDITION 👑           //
-- //              DESIGNED WITH HIGH ELEGANCE & FAST CHAT              //
-- // ================================================================= //

local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TextChatService = game:GetService("TextChatService")
local RunService = game:GetService("RunService")

-- // --- 1. محرك فحص وشات الماب السريع جداً --- //
local CustomChatRemotes = {}

local function ScanRemotes()
    CustomChatRemotes = {}
    local keywords = {"chat", "say", "msg", "message", "talk", "send", "speak", "text", "shout", "cmd"}
    pcall(function()
        for _, obj in ipairs(game:GetDescendants()) do
            if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
                local nameLower = string.lower(obj.Name)
                for _, kw in ipairs(keywords) do
                    if string.find(nameLower, kw) then
                        table.insert(CustomChatRemotes, obj)
                        break
                    end
                end
            end
        end
    end)
end
task.spawn(ScanRemotes)

local function FastSendChat(msg)
    if not msg or msg == "" then return end
    
    -- إرسال عبر ريموتات الماب المكتشفة
    for _, remote in ipairs(CustomChatRemotes) do
        task.spawn(function()
            pcall(function()
                if remote:IsA("RemoteEvent") then
                    remote:FireServer(msg)
                    remote:FireServer(msg, "All")
                elseif remote:IsA("RemoteFunction") then
                    remote:InvokeServer(msg)
                end
            end)
        end)
    end

    -- شات روبلوكس الجديد
    task.spawn(function()
        pcall(function()
            if TextChatService and TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
                local textChannels = TextChatService:FindFirstChild("TextChannels")
                if textChannels then
                    local mainChan = textChannels:FindFirstChild("RBXGeneral") or textChannels:FindFirstChild("All")
                    if mainChan then mainChan:SendAsync(msg) end
                end
            end
        end)
    end)

    -- شات روبلوكس الكلاسيكي
    task.spawn(function()
        pcall(function()
            local chatEvents = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
            if chatEvents then
                local sayRemote = chatEvents:FindFirstChild("SayMessageRequest")
                if sayRemote then sayRemote:FireServer(msg, "All") end
            end
        end)
    end)
end

-- // --- 2. إنشاء الواجهة الأسطورية --- //
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AboodUltimateHub_V30"
ScreenGui.ResetOnSpawn = false

pcall(function()
    if gethui then
        ScreenGui.Parent = gethui()
    elseif syn and syn.protect_gui then
        syn.protect_gui(ScreenGui)
        ScreenGui.Parent = CoreGui
    else
        ScreenGui.Parent = CoreGui or LocalPlayer:WaitForChild("PlayerGui")
    end
end)

-- زر التجميع والفتح/الإغلاق العائم
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Position = UDim2.new(0.02, 0, 0.4, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(15, 10, 25)
ToggleBtn.Text = "👑"
ToggleBtn.TextSize = 24
ToggleBtn.Active = true
ToggleBtn.Draggable = true
ToggleBtn.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner", ToggleBtn) ToggleCorner.CornerRadius = UDim.new(1, 0)
local ToggleStroke = Instance.new("UIStroke", ToggleBtn)
ToggleStroke.Color = Color3.fromRGB(255, 0, 100)
ToggleStroke.Thickness = 2.5

-- الإطار الرئيسي للواجهة
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 580, 0, 420)
MainFrame.Position = UDim2.new(0.5, -290, 0.5, -210)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 8, 16)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner", MainFrame) MainCorner.CornerRadius = UDim.new(0, 16)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(255, 0, 100)
MainStroke.Thickness = 2

-- تأثير أنيميشن إضاءة الحدود (Neon Pulse Glow)
task.spawn(function()
    while task.wait(1.5) do
        TweenService:Create(MainStroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(138, 43, 226)
        }):Play()
        TweenService:Create(ToggleStroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(138, 43, 226)
        }):Play()
        task.wait(1.5)
        TweenService:Create(MainStroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(255, 0, 100)
        }):Play()
        TweenService:Create(ToggleStroke, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
            Color = Color3.fromRGB(255, 0, 100)
        }):Play()
    end
end)

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    MainFrame:ScaleGlow(MainFrame.Visible)
end)

-- شريط العنوان العلوي (Banner)
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundColor3 = Color3.fromRGB(18, 12, 28)
Header.Parent = MainFrame

local HeaderCorner = Instance.new("UICorner", Header) HeaderCorner.CornerRadius = UDim.new(0, 16)

local HeaderTitle = Instance.new("TextLabel")
HeaderTitle.Size = UDim2.new(1, -20, 1, 0)
HeaderTitle.Position = UDim2.new(0, 10, 0, 0)
HeaderTitle.BackgroundTransparency = 1
HeaderTitle.Text = "✨ تم تشغيل سكربت عبود ✨ ABOOD ULTIMATE HUB"
HeaderTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
HeaderTitle.Font = Enum.Font.BuilderSansBold
HeaderTitle.TextSize = 15
HeaderTitle.Parent = Header

-- شريط التبويبات (Tabs)
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -20, 0, 36)
TabBar.Position = UDim2.new(0, 10, 0, 58)
TabBar.BackgroundTransparency = 1
TabBar.Parent = MainFrame

local TabListLayout = Instance.new("UIListLayout", TabBar)
TabListLayout.FillDirection = Enum.FillDirection.Horizontal
TabListLayout.Padding = UDim.new(0, 8)
TabListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -20, 1, -110)
Container.Position = UDim2.new(0, 10, 0, 100)
Container.BackgroundTransparency = 1
Container.Parent = MainFrame

local Tabs = {"🎯 النسخ والتطبيش", "🛡️ الحماية الأسطورية", "⚡ شات الماب والمزيد"}
local Pages = {}
local CurrentTabBtn = nil

for i, tabName in ipairs(Tabs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.31, 0, 1, 0)
    btn.BackgroundColor3 = Color3.fromRGB(25, 18, 38)
    btn.Text = tabName
    btn.TextColor3 = Color3.fromRGB(180, 180, 200)
    btn.Font = Enum.Font.BuilderSansBold
    btn.TextSize = 12
    btn.Parent = TabBar

    local bCorner = Instance.new("UICorner", btn) bCorner.CornerRadius = UDim.new(0, 8)
    local bStroke = Instance.new("UIStroke", btn) bStroke.Color = Color3.fromRGB(50, 35, 70)

    local page = Instance.new("Frame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.Visible = (i == 1)
    page.Parent = Container
    Pages[tabName] = page

    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        for _, b in ipairs(TabBar:GetChildren()) do
            if b:IsA("TextButton") then
                TweenService:Create(b, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(25, 18, 38), TextColor3 = Color3.fromRGB(180, 180, 200)}):Play()
            end
        end
        page.Visible = true
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 0, 100), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
    end)

    if i == 1 then
        btn.BackgroundColor3 = Color3.fromRGB(255, 0, 100)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end
end

-- =================================================================
-- PAGE 1: النسخ السريع الجبار (Target Player Commands)
-- =================================================================
local TargetPage = Pages["🎯 النسخ والتطبيش"]

local SelectedPlayerName = nil

local InfoLbl = Instance.new("TextLabel")
InfoLbl.Size = UDim2.new(1, 0, 0, 25)
InfoLbl.BackgroundTransparency = 1
InfoLbl.Text = "👤 حدد اللاعب المراد النسخ عليه من القائمة:"
InfoLbl.TextColor3 = Color3.fromRGB(255, 215, 0)
InfoLbl.Font = Enum.Font.BuilderSansBold
InfoLbl.TextSize = 12
InfoLbl.Parent = TargetPage

local PlrScroll = Instance.new("ScrollingFrame")
PlrScroll.Size = UDim2.new(1, 0, 0, 160)
PlrScroll.Position = UDim2.new(0, 0, 0, 28)
PlrScroll.BackgroundColor3 = Color3.fromRGB(18, 12, 28)
PlrScroll.ScrollBarThickness = 4
PlrScroll.Parent = TargetPage
local psc = Instance.new("UICorner", PlrScroll) psc.CornerRadius = UDim.new(0, 8)
local pss = Instance.new("UIStroke", PlrScroll) pss.Color = Color3.fromRGB(255, 0, 100)

local PlrListLayout = Instance.new("UIListLayout", PlrScroll)
PlrListLayout.Padding = UDim.new(0, 4)

local StatusLbl = Instance.new("TextLabel")
StatusLbl.Size = UDim2.new(1, 0, 0, 25)
StatusLbl.Position = UDim2.new(0, 0, 0, 195)
StatusLbl.BackgroundTransparency = 1
StatusLbl.Text = "🎯 الهدف المحدد حالياً: لم يتم التحديد"
StatusLbl.TextColor3 = Color3.fromRGB(200, 200, 220)
StatusLbl.Font = Enum.Font.BuilderSansBold
StatusLbl.TextSize = 12
StatusLbl.Parent = TargetPage

local function RefreshPlayers()
    for _, child in ipairs(PlrScroll:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    for _, plr in ipairs(Players:GetPlayers()) do
        local pBtn = Instance.new("TextButton")
        pBtn.Size = UDim2.new(1, -8, 0, 28)
        pBtn.BackgroundColor3 = Color3.fromRGB(28, 20, 42)
        pBtn.Text = "🔥 " .. plr.Name .. " (" .. plr.DisplayName .. ")"
        pBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        pBtn.Font = Enum.Font.BuilderSansBold
        pBtn.TextSize = 11
        pBtn.Parent = PlrScroll
        local c = Instance.new("UICorner", pBtn) c.CornerRadius = UDim.new(0, 6)

        pBtn.MouseButton1Click:Connect(function()
            SelectedPlayerName = plr.Name
            StatusLbl.Text = "🎯 الهدف المحدد حالياً: " .. SelectedPlayerName
            StatusLbl.TextColor3 = Color3.fromRGB(0, 255, 150)
        end)
    end
    PlrScroll.CanvasSize = UDim2.new(0, 0, 0, #Players:GetPlayers() * 32)
end

RefreshPlayers()
Players.PlayerAdded:Connect(RefreshPlayers)
Players.PlayerRemoving:Connect(RefreshPlayers)

-- قوائم الأوامر الخاصة المطلوبة بالضبط
local TargetCommands = {
    ";jail", ";freeze", ";kill", ";respawn", ";refresh", ";void", ";fire", ";bring",
    ";to", ";teleport", ";handTo", ";box", ";dog", ";worm", ";wormify", ";chibify",
    ";plushify", ";freaky", ";frogly", ";spongify", ";bigify", ";creepify", ";dinofy",
    ";fatify", ";ghost", ";squash", ";fat", ";thin", ";giantDwarf", ";dwarf",
    ";headSize", ";bodyTypeScale", ";depth", ";potatoHead", ";char", ";cmdbar", ";logs"
}

-- زر النسخ السريع الجبار بنقرة واحدة
local FastCopyBtn = Instance.new("TextButton")
FastCopyBtn.Size = UDim2.new(1, 0, 0, 48)
FastCopyBtn.Position = UDim2.new(0, 0, 0, 225)
FastCopyBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 80)
FastCopyBtn.Text = "⚡ تفعيل النسخ والتطبيش السريع الجبار (ضغطة واحدة) ⚡"
FastCopyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FastCopyBtn.Font = Enum.Font.BuilderSansBold
FastCopyBtn.TextSize = 13
FastCopyBtn.Parent = TargetPage

local fbc = Instance.new("UICorner", FastCopyBtn) fbc.CornerRadius = UDim.new(0, 10)
local fbs = Instance.new("UIStroke", FastCopyBtn) fbs.Color = Color3.fromRGB(255, 215, 0) fbs.Thickness = 2

FastCopyBtn.MouseButton1Click:Connect(function()
    if not SelectedPlayerName then
        StatusLbl.Text = "⚠️ اختر لاعباً أولاً من القائمة فوق!"
        StatusLbl.TextColor3 = Color3.fromRGB(255, 50, 50)
        return
    end

    StatusLbl.Text = "🚀 جاري الجلد والنسخ السريع على: " .. SelectedPlayerName
    StatusLbl.TextColor3 = Color3.fromRGB(255, 215, 0)

    task.spawn(function()
        for _, cmd in ipairs(TargetCommands) do
            local fullCommand = cmd .. " " .. SelectedPlayerName
            FastSendChat(fullCommand)
            task.wait(0.04) -- سرعة إرسال فائقة
        end
        StatusLbl.Text = "✅ تم إرسال كافة أوامر النسخ بنجاح!"
        StatusLbl.TextColor3 = Color3.fromRGB(0, 255, 150)
    end)
end)

-- =================================================================
-- PAGE 2: الحماية الأسطورية
-- =================================================================
local ProtectPage = Pages["🛡️ الحماية الأسطورية"]

local AutoUnjailBtn = Instance.new("TextButton")
AutoUnjailBtn.Size = UDim2.new(1, 0, 0, 50)
AutoUnjailBtn.Position = UDim2.new(0, 0, 0, 10)
AutoUnjailBtn.BackgroundColor3 = Color3.fromRGB(20, 15, 35)
AutoUnjailBtn.Text = "🛡️ تشغيل حماية فك السجن والتجميد التلقائية (;unjc ;unice)"
AutoUnjailBtn.TextColor3 = Color3.fromRGB(255, 70, 120)
AutoUnjailBtn.Font = Enum.Font.BuilderSansBold
AutoUnjailBtn.TextSize = 12
AutoUnjailBtn.Parent = ProtectPage

local p1c = Instance.new("UICorner", AutoUnjailBtn) p1c.CornerRadius = UDim.new(0, 8)
local p1s = Instance.new("UIStroke", AutoUnjailBtn) p1s.Color = Color3.fromRGB(255, 0, 100)

local Protecting = false
AutoUnjailBtn.MouseButton1Click:Connect(function()
    Protecting = not Protecting
    if Protecting then
        AutoUnjailBtn.Text = "⚡ الحماية الأسطورية شغّالة الآن وتكافح السجن والتجميد! ⚡"
        AutoUnjailBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
        AutoUnjailBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        task.spawn(function()
            while Protecting do
                FastSendChat(";unjc ;unice")
                task.wait(1.5)
            end
        end)
    else
        AutoUnjailBtn.Text = "🛡️ تشغيل حماية فك السجن والتجميد التلقائية (;unjc ;unice)"
        AutoUnjailBtn.BackgroundColor3 = Color3.fromRGB(20, 15, 35)
        AutoUnjailBtn.TextColor3 = Color3.fromRGB(255, 70, 120)
    end
end)

-- =================================================================
-- PAGE 3: شات الماب والمزيد
-- =================================================================
local ExtraPage = Pages["⚡ شات الماب والمزيد"]

local CmdInput = Instance.new("TextBox")
CmdInput.Size = UDim2.new(1, 0, 0, 100)
CmdInput.Position = UDim2.new(0, 0, 0, 10)
CmdInput.BackgroundColor3 = Color3.fromRGB(18, 12, 28)
CmdInput.Text = ";unjc\n;unice"
CmdInput.TextColor3 = Color3.fromRGB(255, 255, 255)
CmdInput.Font = Enum.Font.BuilderSansBold
CmdInput.TextSize = 12
CmdInput.TextYAlignment = Enum.TextYAlignment.Top
CmdInput.Parent = ExtraPage
local cic = Instance.new("UICorner", CmdInput) cic.CornerRadius = UDim.new(0, 8)
local cis = Instance.new("UIStroke", CmdInput) cis.Color = Color3.fromRGB(255, 0, 100)

local SendCmdBtn = Instance.new("TextButton")
SendCmdBtn.Size = UDim2.new(1, 0, 0, 42)
SendCmdBtn.Position = UDim2.new(0, 0, 0, 120)
SendCmdBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
SendCmdBtn.Text = "🚀 إرسال الأوامر عبر شات الماب المخصص"
SendCmdBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SendCmdBtn.Font = Enum.Font.BuilderSansBold
SendCmdBtn.TextSize = 12
SendCmdBtn.Parent = ExtraPage
local smc = Instance.new("UICorner", SendCmdBtn) smc.CornerRadius = UDim.new(0, 8)

SendCmdBtn.MouseButton1Click:Connect(function()
    for line in string.gmatch(CmdInput.Text, "[^\r\n]+") do
        FastSendChat(line)
        task.wait(0.1)
    end
end)
