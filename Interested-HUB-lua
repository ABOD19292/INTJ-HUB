-- // ================================================================= //
-- //         INTJ-HUB : ABOOD CUSTOM CHAT & MAP ENGINE (V22)           //
-- //             SUPPORT FOR CUSTOM MAP-CODED CHAT SYSTEMS             //
-- // ================================================================= //

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TextChatService = game:GetService("TextChatService")
local VirtualUser = game:GetService("VirtualUser")

-- قائمة الريموتات المكشوفة المخصصة للماب
local CustomChatRemotes = {}

-- // --- 1. فاحص ريموتات الشات المخصص للماب (Custom Chat Finder) --- //
local function ScanForCustomChatRemotes()
    CustomChatRemotes = {}
    local keywords = {"chat", "say", "msg", "message", "talk", "send", "speak", "text", "shout", "cmd", "command"}
    
    local function search(parent)
        pcall(function()
            for _, obj in ipairs(parent:GetDescendants()) do
                if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
                    local nameLower = string.lower(obj.Name)
                    for _, kw in ipairs(keywords) do
                        if string.find(nameLower, kw) then
                            table.insert(CustomChatRemotes, obj)
                            break
                        end
                    end
                end
            end
        end)
    end

    search(ReplicatedStorage)
    search(game:GetService("Workspace"))
    search(LocalPlayer)
end

-- تشغيل الفحص الأول عند بدء السكربت
ScanForCustomChatRemotes()

-- // --- 2. محرك الشات الشامل (يدعم شات الماب البرمجي) --- //
local function UniversalSendChat(msg)
    if not msg or msg == "" then return end

    local sent = false

    -- أولاً: تجربة إرسال عبر جميع ريموتات الشات المخصصة التي تم اكتشافها بالماب
    for _, remote in ipairs(CustomChatRemotes) do
        pcall(function()
            if remote:IsA("RemoteEvent") then
                remote:FireServer(msg)
                remote:FireServer(msg, "All")
                remote:FireServer("Message", msg)
            elseif remote:IsA("RemoteFunction") then
                remote:InvokeServer(msg)
                remote:InvokeServer(msg, "All")
            end
            sent = true
        end)
    end

    -- ثانياً: شات روبلوكس الحديث (TextChatService)
    pcall(function()
        if TextChatService and TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            local textChannels = TextChatService:FindFirstChild("TextChannels")
            if textChannels then
                local mainChan = textChannels:FindFirstChild("RBXGeneral") or textChannels:FindFirstChild("All")
                if mainChan then
                    mainChan:SendAsync(msg)
                    sent = true
                end
            end
        end
    end)

    -- ثالثاً: شات روبلوكس القديم (DefaultChatSystemChatEvents)
    pcall(function()
        local chatEvents = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
        if chatEvents then
            local sayRemote = chatEvents:FindFirstChild("SayMessageRequest")
            if sayRemote and sayRemote:IsA("RemoteEvent") then
                sayRemote:FireServer(msg, "All")
                sent = true
            end
        end
    end)
end

-- // --- 3. إنشاء الواجهة الرئيسية --- //
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AboodCustomChatHUB_V22"
ScreenGui.ResetOnSpawn = false

pcall(function()
    if gethui then
        ScreenGui.Parent = gethui()
    elseif syn and syn.protect_gui then
        syn.protect_gui(ScreenGui)
        ScreenGui.Parent = CoreGui
    elseif CoreGui then
        ScreenGui.Parent = CoreGui
    else
        ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    end
end)

-- زر ABOD الجانبي
local ToggleContainer = Instance.new("Frame")
ToggleContainer.Size = UDim2.new(0, 75, 0, 36)
ToggleContainer.Position = UDim2.new(1, -85, 0.5, 10)
ToggleContainer.BackgroundTransparency = 1
ToggleContainer.Parent = ScreenGui

local AboodBtn = Instance.new("TextButton")
AboodBtn.Size = UDim2.new(0, 48, 1, 0)
AboodBtn.BackgroundColor3 = Color3.fromRGB(180, 20, 30)
AboodBtn.Text = "ABOD"
AboodBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AboodBtn.Font = Enum.Font.BuilderSansBold
AboodBtn.TextSize = 13
AboodBtn.Parent = ToggleContainer
local abc = Instance.new("UICorner", AboodBtn) abc.CornerRadius = UDim.new(0, 8)

local ArrowBtn = Instance.new("TextButton")
ArrowBtn.Size = UDim2.new(0, 22, 1, 0)
ArrowBtn.Position = UDim2.new(0, 52, 0, 0)
ArrowBtn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
ArrowBtn.Text = "◄"
ArrowBtn.TextColor3 = Color3.fromRGB(255, 70, 80)
ArrowBtn.Font = Enum.Font.BuilderSansBold
ArrowBtn.TextSize = 12
ArrowBtn.Parent = ToggleContainer
local arc = Instance.new("UICorner", ArrowBtn) arc.CornerRadius = UDim.new(0, 8)

-- النافذة الرئيسية
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 570, 0, 420)
MainFrame.Position = UDim2.new(0.5, -285, 0.5, -210)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 5, 7)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner", MainFrame) MainCorner.CornerRadius = UDim.new(0, 12)
local MainStroke = Instance.new("UIStroke", MainFrame) MainStroke.Color = Color3.fromRGB(180, 20, 30) MainStroke.Thickness = 2

local function ToggleMenu()
    MainFrame.Visible = not MainFrame.Visible
    ArrowBtn.Text = MainFrame.Visible and "◄" or "►"
end
AboodBtn.MouseButton1Click:Connect(ToggleMenu)
ArrowBtn.MouseButton1Click:Connect(ToggleMenu)

-- شريط التبويبات
local NavFrame = Instance.new("Frame")
NavFrame.Size = UDim2.new(1, -20, 0, 36)
NavFrame.Position = UDim2.new(0, 10, 0, 12)
NavFrame.BackgroundTransparency = 1
NavFrame.Parent = MainFrame

local NavLayout = Instance.new("UIListLayout", NavFrame)
NavLayout.FillDirection = Enum.FillDirection.Horizontal
NavLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
NavLayout.Padding = UDim.new(0, 5)

local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -24, 1, -85)
Container.Position = UDim2.new(0, 12, 0, 55)
Container.BackgroundTransparency = 1
Container.Parent = MainFrame

local TabsList = {"شات الماب 🔍", "تخريب على الكل", "Cmdbar2", "حماية", "سكربتات إضافية"}
local TabPages = {}
local ActiveTabBtn = nil

for i, tabName in ipairs(TabsList) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.19, 0, 1, 0)
    btn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
    btn.Text = tabName
    btn.TextColor3 = Color3.fromRGB(180, 150, 155)
    btn.Font = Enum.Font.BuilderSansBold
    btn.TextSize = 11
    btn.Parent = NavFrame

    local bc = Instance.new("UICorner", btn) bc.CornerRadius = UDim.new(0, 7)
    local bs = Instance.new("UIStroke", btn) bs.Color = Color3.fromRGB(50, 15, 22)

    local page = Instance.new("Frame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.Parent = Container
    TabPages[tabName] = page

    btn.MouseButton1Click:Connect(function()
        if ActiveTabBtn then
            ActiveTabBtn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
            ActiveTabBtn.TextColor3 = Color3.fromRGB(180, 150, 155)
            ActiveTabBtn:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(50, 15, 22)
            TabPages[ActiveTabBtn.Text].Visible = false
        end
        ActiveTabBtn = btn
        btn.BackgroundColor3 = Color3.fromRGB(180, 20, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(230, 30, 45)
        page.Visible = true
    end)

    if i == 1 then
        ActiveTabBtn = btn
        btn.BackgroundColor3 = Color3.fromRGB(180, 20, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn:FindFirstChildOfClass("UIStroke").Color = Color3.fromRGB(230, 30, 45)
        page.Visible = true
    end
end

-- =================================================================
-- TAB 1: كاشف ومعدل شات الماب المخصص
-- =================================================================
local CustomChatPage = TabPages["شات الماب 🔍"]

local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(1, 0, 0, 30)
InfoLabel.BackgroundTransparency = 1
InfoLabel.Text = "🛠️ أدوات ربط شات الماب المخصص مع السكربت:"
InfoLabel.TextColor3 = Color3.fromRGB(255, 70, 80)
InfoLabel.Font = Enum.Font.BuilderSansBold
InfoLabel.TextSize = 12
InfoLabel.Parent = CustomChatPage

local ScanBtn = Instance.new("TextButton")
ScanBtn.Size = UDim2.new(1, 0, 0, 36)
ScanBtn.Position = UDim2.new(0, 0, 0, 35)
ScanBtn.BackgroundColor3 = Color3.fromRGB(180, 20, 30)
ScanBtn.Text = "🔍 إعادة فحص واكتشاف ريموتات شات الماب تلقائياً"
ScanBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ScanBtn.Font = Enum.Font.BuilderSansBold
ScanBtn.TextSize = 12
ScanBtn.Parent = CustomChatPage
local sbc = Instance.new("UICorner", ScanBtn) sbc.CornerRadius = UDim.new(0, 6)

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, 0, 0, 45)
StatusLabel.Position = UDim2.new(0, 0, 0, 78)
StatusLabel.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
StatusLabel.Text = "الريموتات المكتشفة حالياً: " .. tostring(#CustomChatRemotes)
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.Font = Enum.Font.BuilderSansBold
StatusLabel.TextSize = 11
StatusLabel.Parent = CustomChatPage
local stc = Instance.new("UICorner", StatusLabel) stc.CornerRadius = UDim.new(0, 6)

ScanBtn.MouseButton1Click:Connect(function()
    ScanForCustomChatRemotes()
    StatusLabel.Text = "✅ تم الفحص! عدد الريموتات المكتشفة: " .. tostring(#CustomChatRemotes)
end)

local SpyBtn = Instance.new("TextButton")
SpyBtn.Size = UDim2.new(1, 0, 0, 36)
SpyBtn.Position = UDim2.new(0, 0, 0, 130)
SpyBtn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
SpyBtn.Text = "🕵️ تشغيل SimpleSpy (لمعرفة اسم ريموت الشات فوراً)"
SpyBtn.TextColor3 = Color3.fromRGB(255, 70, 80)
SpyBtn.Font = Enum.Font.BuilderSansBold
SpyBtn.TextSize = 12
SpyBtn.Parent = CustomChatPage
local spc = Instance.new("UICorner", SpyBtn) spc.CornerRadius = UDim.new(0, 6)
local sps = Instance.new("UIStroke", SpyBtn) sps.Color = Color3.fromRGB(180, 20, 30)

SpyBtn.MouseButton1Click:Connect(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/7Grant9/SimpleSpy/main/SimpleSpySource.lua"))()
    end)
end)

-- =================================================================
-- TAB 2: تخريب على الكل
-- =================================================================
local SabotagePage = TabPages["تخريب على الكل"]

local CmdBox = Instance.new("TextBox")
CmdBox.Size = UDim2.new(1, 0, 0, 40)
CmdBox.Position = UDim2.new(0, 0, 0, 0)
CmdBox.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
CmdBox.Text = ";kill all"
CmdBox.TextColor3 = Color3.fromRGB(255, 70, 80)
CmdBox.Font = Enum.Font.BuilderSansBold
CmdBox.TextSize = 12
CmdBox.Parent = SabotagePage
local cbc = Instance.new("UICorner", CmdBox) cbc.CornerRadius = UDim.new(0, 6)
local cbs = Instance.new("UIStroke", CmdBox) cbs.Color = Color3.fromRGB(180, 20, 30)

local function CreateSabButton(text, yPos, isRed, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 32)
    btn.Position = UDim2.new(0, 0, 0, yPos)
    btn.BackgroundColor3 = isRed and Color3.fromRGB(180, 20, 30) or Color3.fromRGB(20, 8, 12)
    btn.Text = text
    btn.TextColor3 = isRed and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 70, 80)
    btn.Font = Enum.Font.BuilderSansBold
    btn.TextSize = 12
    btn.Parent = SabotagePage
    local c = Instance.new("UICorner", btn) c.CornerRadius = UDim.new(0, 6)
    local s = Instance.new("UIStroke", btn) s.Color = Color3.fromRGB(180, 20, 30)
    btn.MouseButton1Click:Connect(callback)
    return btn
end

local MassActive = false
CreateSabButton("💥 إرسال التخريب المكتوب عبر شات الماب", 48, true, function()
    UniversalSendChat(CmdBox.Text)
end)

CreateSabButton("💀 إبادة وقتل الجميع (;kill all)", 86, false, function() UniversalSendChat(";kill all") end)
CreateSabButton("⛓️ سجن جميع اللاعبين (;jail all)", 124, false, function() UniversalSendChat(";jail all") end)
CreateSabButton("❄️ تجميد السيرفر بالكامل (;freeze all)", 162, false, function() UniversalSendChat(";freeze all") end)
CreateSabButton("🐛 تحويل الكل لديدان (;worm all)", 200, false, function() UniversalSendChat(";worm all") end)

-- =================================================================
-- TAB 3: Cmdbar2
-- =================================================================
local CmdBarPage = TabPages["Cmdbar2"]

local CmdBarInput = Instance.new("TextBox")
CmdBarInput.Size = UDim2.new(1, 0, 0, 110)
CmdBarInput.Position = UDim2.new(0, 0, 0, 10)
CmdBarInput.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
CmdBarInput.Text = ";unjc\n;unice"
CmdBarInput.TextColor3 = Color3.fromRGB(255, 255, 255)
CmdBarInput.Font = Enum.Font.BuilderSansBold
CmdBarInput.TextSize = 12
CmdBarInput.TextYAlignment = Enum.TextYAlignment.Top
CmdBarInput.Parent = CmdBarPage
local cbic = Instance.new("UICorner", CmdBarInput) cbic.CornerRadius = UDim.new(0, 6)
local cbis = Instance.new("UIStroke", CmdBarInput) cbis.Color = Color3.fromRGB(180, 20, 30)

local CmdBarSendBtn = Instance.new("TextButton")
CmdBarSendBtn.Size = UDim2.new(1, 0, 0, 38)
CmdBarSendBtn.Position = UDim2.new(0, 0, 0, 130)
CmdBarSendBtn.BackgroundColor3 = Color3.fromRGB(180, 20, 30)
CmdBarSendBtn.Text = "⚡ إرسال عبر شات الماب المخصص"
CmdBarSendBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CmdBarSendBtn.Font = Enum.Font.BuilderSansBold
CmdBarSendBtn.TextSize = 12
CmdBarSendBtn.Parent = CmdBarPage
local cbc1 = Instance.new("UICorner", CmdBarSendBtn) cbc1.CornerRadius = UDim.new(0, 6)

CmdBarSendBtn.MouseButton1Click:Connect(function()
    for line in string.gmatch(CmdBarInput.Text, "[^\r\n]+") do
        UniversalSendChat(line)
        task.wait(0.2)
    end
end)

-- =================================================================
-- TAB 4: حماية
-- =================================================================
local ProtectPage = TabPages["حماية"]

local ProtToggleBtn = Instance.new("TextButton")
ProtToggleBtn.Size = UDim2.new(1, 0, 0, 36)
ProtToggleBtn.Position = UDim2.new(0, 0, 0, 0)
ProtToggleBtn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
ProtToggleBtn.Text = "🛡️ حماية فك السجن والتجميد تلقائياً (;unjc ;unice)"
ProtToggleBtn.TextColor3 = Color3.fromRGB(255, 70, 80)
ProtToggleBtn.Font = Enum.Font.BuilderSansBold
ProtToggleBtn.TextSize = 12
ProtToggleBtn.Parent = ProtectPage
local ptc = Instance.new("UICorner", ProtToggleBtn) ptc.CornerRadius = UDim.new(0, 6)
local pts = Instance.new("UIStroke", ProtToggleBtn) pts.Color = Color3.fromRGB(180, 20, 30)

local IsProtecting = false
ProtToggleBtn.MouseButton1Click:Connect(function()
    IsProtecting = not IsProtecting
    if IsProtecting then
        ProtToggleBtn.Text = "🛡️ الحماية الخارقة شغالّة وتعمل تلقائياً! ⚡"
        ProtToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 20, 30)
        ProtToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        task.spawn(function()
            while IsProtecting do
                UniversalSendChat(";unjc ;unice")
                task.wait(1.8)
            end
        end)
    else
        ProtToggleBtn.Text = "🛡️ حماية فك السجن والتجميد تلقائياً (;unjc ;unice)"
        ProtToggleBtn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
        ProtToggleBtn.TextColor3 = Color3.fromRGB(255, 70, 80)
    end
end)

-- =================================================================
-- TAB 5: سكربتات إضافية
-- =================================================================
local ExtraPage = TabPages["سكربتات إضافية"]

local ExtraGrid = Instance.new("UIGridLayout", ExtraPage)
ExtraGrid.CellSize = UDim2.new(0.48, 0, 0, 36)
ExtraGrid.CellPadding = UDim2.new(0.04, 0, 0, 8)

local function AddScriptButton(name, code)
    local btn = Instance.new("TextButton")
    btn.Text = name
    btn.BackgroundColor3 = Color3.fromRGB(20, 8, 12)
    btn.TextColor3 = Color3.fromRGB(255, 70, 80)
    btn.Font = Enum.Font.BuilderSansBold
    btn.TextSize = 11
    btn.Parent = ExtraPage
    local c = Instance.new("UICorner", btn) c.CornerRadius = UDim.new(0, 6)
    local s = Instance.new("UIStroke", btn) s.Color = Color3.fromRGB(180, 20, 30)
    btn.MouseButton1Click:Connect(function()
        pcall(function() loadstring(code)() end)
    end)
end

AddScriptButton("Infinite Yield 👑", "loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()")
AddScriptButton("Fly Script 🕊️", "loadstring(game:HttpGet('https://raw.githubusercontent.com/jscripty/FlyScript/main/fly.lua'))()")
